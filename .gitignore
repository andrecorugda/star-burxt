# `burxt fetch` writes these; the LOCKFILE is what gets committed, not the checkout.
.burxt/

# Built from examples/generate.bx by the set-up page and by CI.
star-generate

# Generated beside their source by star-generate.
examples/*.bx
examples/*.check.bx
star-check
